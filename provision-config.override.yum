distillery_base_domain: rnd-ideaproject.huawei.com
#distillery_base_domain: project.hao.com

proxy_peer: "true"
proxy_peer_address: "qeesung.host.huawei.com"
proxy_peer_port: 3128

#proxy_peer_address: "proxyde.huawei.com"
#proxy_peer_port: 8080
#proxy_peer_username: "h00283784"
#proxy_peer_password: "!jiki123"
#proxy_internal_network: 'all'

longrow_retention_days: 31

###
# DNS forwarder configuration
###
# this value does not need to be set, then bind default recursors to root level DNS servers
# (so domains from company internal DNS servers are not seen.). A list internal (forwarding)
# DNS servers can be given here. Typically the entries from `/etc/resolv.conf` modulus `127.0.0.1`
#
bind_forwarders:
  - 10.72.255.100
  - 10.72.55.82
